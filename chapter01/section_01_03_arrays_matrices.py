# =========================================================
# Section (1.3) 배열(Array)과 행렬(Matrix)
# =========================================================

# Python list
a = [1, 2, 3, 4]
b = [5, 6, 7, 8]

print(a[0])
print(a[-1])

# NumPy array
import numpy as np

A = np.array([1, 2, 3])
B = np.array([4, 5, 6])

print(A + B)

# 2D array
M = np.array([[1, 2], [3, 4]])
print(M)
